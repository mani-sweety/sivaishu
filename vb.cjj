#include<stdio.h>
#include<conio.h>

int main()

{
int i,num,el[20],res;
clrscr();
printf("enter the number of elements:");
scanf("%d",&num);
printf("enter the elements of array:");
for(i=0;i<num;i++)
scanf("%d",&ele[i]);
result=el[0];
for(i=1;i<num;i++)
res^=el[i];

printf("unique element is :%d",res);

getch();
}
]
