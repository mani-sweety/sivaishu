#include<stdio.h>
void main()
{
int num;
printf("enter the number:\n");
scanf("%d"'&num);
if(num>0)
printf("%d is a positive number \n",num);
elseif(num<0)
printf("%d is a negative number\n",num);
else
printf("zero is a negastive or positive");
}
